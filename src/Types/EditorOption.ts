export type EditorOption = {
    snapping: boolean;
    gridSize: number;
    handleRadius: number;
    spareScale: number;
    lineColor: string;
    lineWidth: number;
    measureColor: string;
    shortClickThreshold: number;
};
